# Acoustic sensor simulator Dockerfile

FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY acoustic_simulator.py .

CMD ["python", "acoustic_simulator.py"]
